# Contributing

Use conventional commits and keep UI changes aligned with the monochrome SmartCourt design system.

1. Run `npm run lint`.
2. Run `npm run build`.
3. Add focused Vitest or Playwright coverage for changed behavior.
